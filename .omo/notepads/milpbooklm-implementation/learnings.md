# Learnings — milpbooklm-implementation

Conventions, patterns, and successful approaches discovered during work on this plan.

_Auto-scaffolded by /start-work. Append new entries below - never overwrite._

---

## FND-01 (2026-09-18)

1. **import-linter 2.15 config format**: contracts live in an array `[[tool.importlinter.contracts]]`, NOT old `[tool.importlinter.contract:name]` tables (tables parse but yield 0 contracts). `forbidden` contracts use `source_modules` (not `modules`); `independence` uses `modules`; `layers` uses `layers`. Forbidding external framework modules (fastapi, ...) additionally requires `include_external_packages = true` at top level, or the run aborts with "must have include_external_packages=True". CLI needs the explicit subcommand: `uv run import-linter lint`.

2. **uv workspace + virtual root**: the monorepo root is `[tool.uv] package = false` with zero own deps, so plain `uv sync --frozen` installs almost nothing — use `uv sync --frozen --all-packages` so all six workspace members land in the venv (import-linter and pytest need the installed graph).

3. **pytest collection vs spec-mandated test paths**: `requirements.generated.json` mandates test files like `ver-arch-01-001.py`, which the default `python_files = test_*.py` pattern never collects (0 items, exit 5). Fixed via `python_files = ["test_*.py", "ver-*.py"]` in `[tool.pytest.ini_options]`.

4. **Biome 2.x scope discipline**: `biome check .` formats EVERYTHING, including orchestrator state (`.omo/`), generated evidence (`artifacts/`) and the fingerprinted normative guide — reformatting the guide would break ledger fingerprints. Excluded all three via `files.includes` negations; run `biome migrate --write` when the CLI version outgrows the `$schema` (2.0.0 → 2.5.14, `recommended` → `preset`).

5. **TS const + same-name type export**: `export type { X }` + `export { X }` for a const-with-type-alias `X` is a duplicate identifier (TS2300) because a plain `export { X }` carries both meanings. Keep the value re-export alone; drop X from the type-only list. Also: biome's organize-imports assist is the safe way to get `verbatimModuleSyntax`-clean import ordering (`type`-specifier first).

6. **pnpm strict layout + project references**: workspace deps are symlinked only into the CONSUMER's node_modules. If `apps/web` imports `@milpbooklm/web-domain` types directly, it must declare `@milpbooklm/web-domain: workspace:*` in its own package.json (and `pnpm install` to update the lock) — otherwise `tsc --build` fails TS2307 even with correct `references`.

7. **CI scripts must own their toolchain PATH — ambient PATH is untrustworthy**: the first `ci/run.sh` pass was a false green: my session had `~/.local/node/current/bin` exported, but a fresh shell resolves `node` to pi-node v22.23.1 (its bin dir comes FIRST in PATH, before `~/.local/bin`) or to `/usr/bin/node` v20.18.1 (login shell), and `uv` (a pyenv shim at `~/.pyenv/shims/uv`) is absent from non-interactive PATHs entirely. Fix: `ci/run.sh` pins `NODE_HOME=$HOME/.local/node/node-v24.21.0-linux-x64` (self-heals from the official tarball if missing), prepends `$NODE_HOME/bin`, and resolves the real uv binary from `~/.pyenv/versions/*/bin/uv` when `command -v uv` fails. Verified with `env -i` clean login shell AND a pi-node-first PATH — both exit 0. Never assert a tool version in CI without first owning its PATH resolution.

---

## FND-02 (2026-09-18)

1. **spec census fingerprint/anchor semantics**: the fingerprint (SHA-256 of source+term+occurrence_on_line+whitespace-normalized statement) deliberately EXCLUDES the line number, so the ledger can survive reflow; line movement is caught only by comparing the anchor (line, occurrence_on_line) of the fingerprint-matched occurrence. Consequence: a -1/+1 mutation that nets zero movement is by design invisible — mutation QA must use insert-only probes. Exact rescan recipe that reproduces all 507 fingerprints: files = guide top-level `[0-2][0-9]-*.md` + `architecture-baseline/[0-2][0-9]-*.md`; regex `\bMUST NOT\b|\bMUST\b|\bSHOULD NOT\b|\bSHOULD\b` IGNORECASE (longest-first), finditer per line, 1-based line and occurrence.

2. **frozen ch02 table: the `Area` column, not `Target capability`, is the identity**: registry `name` maps to the table's Area column; matching on the Target capability column fails 43/43. `reference_text` is a curated approximation of the Target capability cell (e.g. `public_notebooks` differs), so the reparse gate asserts presence/uniqueness + priority→classification only — never byte-equality of reference text. Priority→classification keys are normalized with `re.sub(r'[^a-z0-9]+','',s.lower())`.

3. **shared working tree with a sibling task**: import-linter (and the task-1 import-linter tests) abort with "Syntax error in <untracked sibling WIP file>" while sibling task 3 edits `packages/adapters/` in the same tree. A boundary test must distinguish: "Contract violated" in output = always fail; "Syntax error in" = evaluation blocked, fall back to asserting the frozen contracts are still declared in pyproject.toml (CI's clean checkout re-runs full enforcement). Also: `uv run mypy` over the whole workspace fails on the sibling's broken file — verify owned code with `uv run mypy --package <pkg>`.

4. **script mains and tools-as-modules**: `main(argv)` must check `argv[1:]` (argv[0] is the script path — off-by-one prints usage on the first real run). tools/spec scripts use top-level sibling imports (`from check_capabilities import ...`) which work only in script mode (script dir = sys.path[0]); pytest VER tests load them via `tests/meta/_tools.py::load_tool` (importlib, module name `tools_spec_<name>` to avoid clashing with task-1's registered "extract_requirements"). Deps for tools (PyYAML/jsonschema) + pydantic live in `apps/api/pyproject.toml` — the root pyproject is virtual (`package = false`) and fenced; the shared `.venv` makes them importable from tools.

5. **ruff gate shape**: tools get D/ANN enforced (every `__init__` needs a one-line docstring for D107, private fns need return annotations ANN202), tests ignore D/S/PLC0415/N999. Complexity ceilings bite on gate scripts: C901=10 and PLR0912=12 — split per-item checks into `_check_<unit>` helpers returning `list[Finding]` and compose with `findings.extend(...)`; also PLR2004 (magic numbers → named constants with the normative origin in a comment), PT018 (split compound asserts), PERF401 (comprehension/generator over append loops).

---

## FND-03 (2026-09-19)

1. **`GRANT … ON DATABASE current_database()` is INVALID SQL**: GRANT takes a literal object name, not an expression — `current_database()` inside a GRANT is a syntax error (caught only when the smoke ran roles.sql with `ON_ERROR_STOP=1`; plain `psql -f` continues past the error and hides it). The DB-agnostic pattern that works: issue the DATABASE-level grants from a DO block via dynamic SQL — `EXECUTE format('GRANT ALL ON DATABASE %I TO milpbooklm_migration', current_database());` — while schema-level grants (`GRANT … ON SCHEMA public`) stay plain statements (the schema name is constant per DB). Keep the comment explaining WHY the indirection exists, or a future cleanup reverts it to the broken form.

2. **Immutable tables make test-harness cleanup impossible as the app role**: the 9 fully-immutable tables (BEFORE UPDATE OR DELETE → RAISE) mean a harness that cleans up by NULL-ing FKs (UPDATE) or deleting rows (DELETE) is blocked by the invariants it just verified — that is the invariants WORKING, not the schema being broken. 13/20 invariants tests failed exactly this way in cleanup after their body assertions passed. The wave-2 fix: run cleanup over a SUPERUSER connection with `ALTER TABLE <t> DISABLE TRIGGER USER` on the 9 tables → LIFO deletes (children before parents, since FK cascades fire row triggers too) → re-enable. Never put DB drops in a per-test fixture (3 conftests each run ensure_pg → 3x per session).

3. **/run/user is tmpfs — a host reboot silently wipes the whole direct-binary PG setup** (rootfs, hostlibs, pgdata, all of it) while the repo on disk survives. Rebuild recipe (all in /tmp/opencode/fnd03-scratch/): `rebuild-scratch.sh` (pull the 15 OCI layers of digest sha256:1d50c689… via auth.docker.io token API, extract usr/lib/postgresql* + usr/share/postgresql*), `fetch-deb-libs.sh` (bookworm .debs for libicu72/libldap-2.5-0/liburing2 — liblber-2.5.so.0 ships INSIDE libldap-2.5-0, there is no standalone bookworm liblber package), `bringup2.sh` (initdb + pg_ctl on 29517). Also: the rootfs `psql` does NOT work (needs the image's libpq.so.5, which we don't ship in LD_LIBRARY_PATH) — use the host psql (a PG17 client talks to a PG18 server fine) for ad-hoc SQL.

4. **One-time state transitions interact with FK-nulling cleanup**: the fixed `milpbooklm_evidence_snapshot_guard` (one-time promotion: once `promoted_source_version_id IS NOT NULL`, any change RAISEs) correctly blocks a cleanup that tries to `SET promoted_source_version_id = NULL` before deleting the referenced source version. Design rule for such guards: they make the promoted row unlinkable except via DELETE of the row itself — the table must stay deletable (no immutability trigger) or cleanup must go through the privileged path. Probe evidence: .omo/evidence/task-3-migration-smoke.log + the 7/13 probe split in .omo/evidence/task-3-milpbooklm-implementation.json.

---

## FND-04 (2026-09-19)

1. **The inherited "3 QA failures" were all defects of the LOST driver, not the code** — the rebuilt driver (same 40 checks) is 100% green with zero code changes. Root causes: (a/b) the lost driver re-sent a REVOKED token after the owner re-login (401 "authentication required" is the CORRECT answer); (c) its rate-limit probe reused an email whose `login:<ip>:<email>` limiter key already held one attempt from the disabled-owner login check (the 429-on-the-6th is exactly the configured `login_max_attempts=5` behavior). Lesson: before "fixing" a dead worker's failing QA, reproduce with a clean driver — the failure may live in the harness.

2. **Limiter keys are per-IP-per-email and count EVERY attempt (successes too)** — a QA flow that logs the same account in more than 5 times in 15 min (or registers more than 3 accounts per IP/hour, the configured register default) gets uniform 429s. QA recipes: rate-limit probes need a FRESH account (seed it via SQL with a real argon2id hash — the check exercises its LOGIN key, not registration), and multi-account flows should clear the cookie jar between users so the register budget and the CSRF middleware don't entangle with the check under test.

3. **CSRF middleware intercepts EVERY unsafe /api/v1 request carrying a live session cookie — including POST /login** (the middleware runs before the route). So an authn probe like "disabled owner cannot log in" must run with an EMPTY cookie jar, or it gets 403 csrf_rejected before authn runs. Also: session cookies are Secure, so TestClient needs an https base_url (unit tests use `https://testserver`) or the jar silently drops them.

4. **QA PG after the /tmp wipe: host PG17 + a 4-line uuidv7() SQL shim is enough** — the only PG18-specific element in the FND-03 baseline DDL is the `uuidv7()` PK default; a scratch-only shim (48-bit unix-ms timestamp | version 7 | variant 10 | random) makes the whole baseline migrate on the host PostgreSQL 17 + pgvector 0.8.2 (~110ms, port 29518). Keep the shim OUT of the repo. Also: `make_engine()` picks the DBAPI from the DSN scheme — `postgresql://` means psycopg2 (NOT installed); the engine DSN must be `postgresql+psycopg://` while psycopg3-direct (harness/driver fixtures) accepts the plain scheme.

---

## FND-05 (2026-09-20)

1. **Starlette route registration order is load-bearing with typed path params**: a static segment registered AFTER `/{job_id}` (uuid.UUID) is shadowed — the `[^/]+` pattern matches "events" first, UUID validation 422s, and Starlette never falls through to the later `/events` route (silent, no warning). Register static paths before parameterized ones (job_routes.py: /events before /{job_id}).

2. **Read the T3 CheckConstraints BEFORE writing terminal transitions**: `ck_jobs_lease_consistency` (`lease_owner IS NULL OR status IN ('leased','running')`) made complete()/cancel() crash on the first happy-path publish — the recovery path had the lease NULL-clears, the happy path didn't. Related: `idempotency_keys.expires_at` is NOT NULL with NO server default — `pg_insert(...).values()` only sets what you pass, and a defined-but-unused TTL constant (`_IDEMPOTENCY_TTL_SECONDS`) is a bug magnet.

3. **rtk + background processes gotchas**: `cmd > file 2>&1 &` through rtk loses the redirection (the process runs, logs stay empty forever); route background boots through a script file (`setsid bash start.sh > log`) whose contents rtk does not rewrite. And `pkill -f "pattern"` self-matches the invoking `bash -c` line — use the bracket trick (`milpbooklm_[a]pi`) so the pattern string in your own cmdline doesn't match; a single missing character in the pattern (an underscore!) silently kills nothing, and the next bind then fails with EADDRINUSE while the stale server serves the old code.

4. **Worker kill for lease-recovery QA**: SIGKILL of the process-group head (the rtk/uv wrapper) orphans the real python child (rtk -> uv -> python3); `pgrep -f` on a CLI arg (`--worker-id w1`) catches all three tiers at once. In a recovery scenario ANY other live worker can be the one that recovers (in the actual run, the surviving w1 — not the freshly spawned w2b — took the job), so assertions must be owner-agnostic: check attempt rows for "[killed owner: running] [other owner: succeeded]", not a specific worker id.

---

## FND-06 (2026-09-20)

1. **`initdb --no-locale` silently produces an SQL_ASCII server, which makes psycopg3 return text columns as `bytes`** — SQLAlchemy 2.0.54 then crashes in its dialect initialize with `TypeError: cannot use a string pattern on a bytes-like object` (`_get_server_version_info` regex-matches `select pg_catalog.version()`). The symptom masquerades as a SQLAlchemy/psycopg incompatibility; the cure is `initdb --encoding=UTF8 --locale=C` (locale C + UTF8 is a valid combo). Every scratch cluster for this repo must be UTF8.

2. **`ck_blob_objects_finalized_timestamp` is load-bearing for GC**: it is `(state = 'finalized') = (finalized_at IS NOT NULL)`, so moving a row to `purged` while keeping `finalized_at` set VIOLATES it. `mark_purged` must set `state='purged'` and `finalized_at=NULL` in the same UPDATE (FND-05 lesson 2 again: read the CheckConstraints before writing the transition).

3. **`os.link` is the atomic no-overwrite finalize; `os.rename` is not allowed here** — rename silently replaces an existing target, which would mutate a finalized blob. link() is atomic on POSIX and raises EEXIST when the target exists; the race-winner path is safe by content addressing (verify the existing object's digest+size, then dedupe). Directory fsync (open O_RDONLY|O_DIRECTORY + fsync) is required after the link for durability.

4. **The put protocol has no staging-row-first two phase**: `blob_objects.storage_path` is NOT NULL and digest-derived, so the row cannot exist before the write. The guide's ordering is followed exactly (temp -> validate -> fsync -> link -> dir fsync -> ONE-transaction object+reference commit); a crash between finalize and commit leaves a complete self-verifying file with no record (an unreferenced final), which GC handles after the safety delay. `commit_finalized` reuses an existing row for the same digest and completes a stale `staging` row (re-finalize) in that case.

5. **Worker maintenance CLI = the durable blob maintenance path without API surface**: `python -m milpbooklm_workers --command {reconcile,put-blob,get-blob,gc}` reuses the same `BlobPorts` wiring as the loop's `blob.integrity_scan` job kind (registered only when `--blob-root` is set). Reuse of a variable name with a different type in one function (`report = ports.reconcile()` ... `report = ports.gc()`) is a mypy `[assignment]` error — name the second one `gc_report`.

6. **Smoke DSN gotcha re-hit**: `make_engine("postgresql://...")` selects psycopg2 (not installed) — engine DSNs must be `postgresql+psycopg://` (FND-04 lesson 4). Also, bare uuid literals in psql (`66666666-7777-...`) parse as arithmetic; quote + `::uuid`.

7. **The FND-03 finalize-guard trigger makes `finalized` TERMINAL — no code may UPDATE a finalized `blob_objects` row, not even to `purged`** (takeover finding). `trg_blob_objects_finalize_guard` (declared in `db/triggers.py`, applied by the FND-03 baseline) raises on any UPDATE with `OLD.state='finalized' AND NEW.state <> 'finalized'` — the RAISE message says "may only be purged", but the logic blocks purged too; the logic is the law. Consequences: (a) SQLAlchemy `Table` metadata shows ZERO triggers — reading `tables/blobs.py` alone is not enough, read `db/triggers.py` before writing any state transition (FND-05 lesson 2 extended); (b) the abandoned T6 `mark_purged` crashed the first GC over a record-based unreferenced final — and its smoke missed this because its aged final was FILE-ONLY (no row → mark_purged never called); (c) T6's GC therefore deletes files only, and a finalized record whose file is gone is a permanent bookkeeping remainder (reported as unreferenced_final with `file_present=False`, settled/skipped by GC, never re-mutated). The `purged` state is reachable only via INSERT (the trigger is BEFORE UPDATE) — owned by a future privileged purge path, not by GC.

8. **GC over record-only findings needs an explicit file-present discriminator**: a record whose object file is already absent has no bucket dir (`objects/<aa>/`), so `delete_final` must guard the directory fsync (`path.parent.is_dir()`) — an unconditional `os.open(O_DIRECTORY)` raises `FileNotFoundError` mid-GC. The discriminator lives on the finding itself (`ReconciliationFinding.file_present`) set by the classifier that knows which artifact space (disk listing vs DB rows) produced it — not re-derived from detail strings.

9. **Verification-found (manual review, fixed in the follow-up `fix(storage)` commit on top of 2bb96eb): two crash/concurrency defects the gates and the first smoke both missed.** (a) GC swept EVERY listed temp immediately, so a temp of a concurrent in-flight `put` (visible on disk while writing/validating) could be unlinked from under the live writer. The fix is inventory, not name-shape: `list_temporaries()` returns `TemporaryStats` (relative_path + mtime), the mtime becomes the finding's age anchor, and the SAME configured safety delay gates the sweep — an active put keeps its temp's mtime fresh, so a fresh temp can never age out. One coherent policy (no second "temp grace" knob) is the only policy that cannot be misconfigured. (b) Fsync-ordering traps: the tmp-dir fsync sat inside `_finalize`, i.e. BEFORE `put()`'s `finally: temp.unlink(...)` — an fsync that precedes the mutation it is supposed to cover is a no-op, while the docstring claimed "after the unlink"; and a newly created `objects/<aa>` bucket was fsynced without its parent, so a crash could lose the bucket's directory entry while the DB row already points at it. Fixes: tmp-dir fsync in `put()`'s finally AFTER the unlink; `objects/` fsynced when (and only when) the bucket was newly created. Lesson: verify "fsync after X" claims against the CALL GRAPH, not the file — and review concurrency-sensitive sweep paths for the in-flight-writer case, which a single-process smoke cannot trigger. Follow-up hardened both: the conditional `bucket_created` parent fsync became unconditional (a concurrent process can create the bucket between the existence check and the mkdir, so the stale observation is not a valid cross-process durability proof), and `delete_temporary` now fsyncs `tmp/` after the unlink so an acknowledged sweep is durable.

## FND-07 (2026-09-20)

1. **Aborted-transaction upsert trap**: an INSERT that raises `IntegrityError` aborts the whole DB transaction server-side — a try/except fallback UPDATE in the SAME `engine.begin()` block then fails with `psycopg.errors.InFailedSqlTransaction` ("current transaction is aborted"). The atomic form is one statement: `pg_insert(...).on_conflict_do_update(index_elements=[...], set_={...})`. Caveat: the conflict target only matches rows when the unique index is NULLS NOT DISTINCT (PG15+) — the baseline's `uq_provider_credentials_config_owner` is, so installation-level NULL-owner rows upsert correctly.
2. **PyNaCl `Aead.encrypt` returns `nonce || ciphertext`, not the ciphertext**: `nacl.secret.Aead.encrypt` returns an `EncryptedMessage`, which is a `bytes` SUBCLASS containing the 24-byte nonce prefixed to the real ciphertext. Passing it straight to a DB column "works" (it is bytes, mypy is happy, the INSERT succeeds) and every later decrypt fails AEAD authentication — the smoke caught it as "credential AEAD authentication failed" on freshly stored rows. Strip the leading `NONCE_BYTES` (`bytes(sealed)[NONCE_BYTES:]`) before persisting. General lesson: a `bytes` subclass is not "the bytes you think" — check what the library returns before it crosses a persistence boundary.
3. **`psql -tAc "INSERT ... RETURNING id"` prints the command tag (`INSERT 0 1`) on a SECOND line after the row** — a two-line capture breaks uuid parsing. Take the first line in pure bash: `var=${var%%$'\n'*}` (no sed/head). SELECTs are single-line and unaffected.
4. **`ver-arch-03-001.py`'s `ALLOWED_ADAPTER_IMPORTS` is policy data, not test logic**: the set mirrors REFERENCE-DEPENDENCIES.md (pattern: `argon2`). Adding a task-mandated dependency (`nacl`) there is a policy update in the same spirit as the argon2 entry — flag it transparently in the task report as the one test-file touch, and keep behavioral assertions untouched.
5. **Keyring/crypto smoke fixtures must be idempotent**: regenerating master keys (openssl rand) on every script run orphans previously stored ciphertexts (they were encrypted under the old key bytes) and the smoke then "fails" for fixture reasons, not product reasons. Generate fixtures only-if-absent and wipe the rows they feed (`DELETE FROM provider_credentials`) at the start of the run.
6. **mypy Protocol vs concrete `@property`**: a Protocol member declared as a plain attribute (`active_key_id: str`) means "settable variable" and does NOT match a concrete read-only `@property`; declare the Protocol member with `@property`/`@abstractmethod` to match. Related narrowing pattern that survived mypy strict: `if x is not None and check(x): y: str = x` — the annotated local is the reliable way to narrow `str | None` through a predicate.
7. **Secure-cookie API smokes need explicit cookie presentation**: the session cookie is `Secure`, so plain-HTTP curl (and httpx's jar) will never present it; TestClient works only when the smoke extracts `r.cookies['mb_session']` after login and sends `Cookie: mb_session=...` explicitly. This is the FND-04 recipe and it is the right shape — it also documents that real browsers only send the cookie over HTTPS, as designed.
8. **A redacting root handler does not make logging leak-safe**: any pre-existing handler on an application package or child logger emits before propagation, creating an unredacted duplicate. Configuration must detach every handler in each owned logger namespace, make descendants propagate to one redacting package handler, and stop propagation at that package boundary; unrelated host/root loggers remain untouched.
9. **Keyring permissions must be checked on the opened descriptor**: `path.stat()` followed by `path.read_text()` has a replacement race and can validate a different inode from the one read. Open once, inspect `os.fstat(file.fileno())` before reading, and keep errors path-free so an actionable permission failure does not disclose secret-file locations.
10. **Unhandled 500 headers need an outer user-ASGI boundary**: Starlette's server error wrapper sits outside user middleware, so ordinary correlation/security middlewares do not decorate its fallback response. An outermost plain-ASGI user middleware can catch before that wrapper, emit a uniform body, and attach the same correlation and security baseline centrally.
11. **ContextVar reset and outer error recovery need separate lifetimes**: a correlation middleware must reset its ContextVar in `finally`, but an outer error boundary still needs the already-validated effective identity after unwinding. Persist the complete effective context tuple in per-request ASGI state before downstream execution, then rebind that exact tuple only while logging/emitting the 500; regenerate solely when the correlation middleware never ran.

## OPS-01a (2026-09-20)

1. **A path setting ending in `_FILE` collides with the generic secret-file resolver**: `resolve_file_secrets()` treats every `MILPBOOKLM_*_FILE` as an instruction to read the named file and replaces it with the suffix-free key. The installer report path must therefore be `MILPBOOKLM_PREREQUISITES_PATH`; using `MILPBOOKLM_PREREQUISITES_FILE` became the unknown key `MILPBOOKLM_PREREQUISITES` and correctly failed startup.
2. **A private application network and SearXNG internet egress are separate concerns**: keeping the backend network `internal: true` prevents accidental service egress as well as ingress. SearXNG needs a second unpublished egress network while every other data/application service remains only on the internal backend network.
3. **Record accelerator overrides alongside detected architectures**: PCI identifies both Radeon VII cards as Vega 20 (`1002:66af`, native gfx906), while this host exports `HSA_OVERRIDE_GFX_VERSION=9.0.0`; ROCm 7.15 consequently reports both cards as gfx900. Deployment facts must preserve both observations so later ADR benchmarks do not mistake the override for the physical architecture.
4. **`systemd-analyze verify` distinguishes syntax from absent runtime artifacts**: after correcting the condition key to `ConditionFileIsExecutable`, the example units parse; on this host it then reports only the deliberately absent `/usr/bin/podman` and task-29 broker executable. Those are runtime prerequisite failures, not unit-file syntax failures.

## SCOPE-GUARD (2026-09-20)

1. **Package the reviewed registry instead of copying capability IDs into runtime code**: relocating the canonical YAML under `milpbooklm_contracts` lets `importlib.resources` work in editable installs and built wheels while the existing spec checker still compares that same file to `capabilities.generated.json`. Runtime code parses only the fields it consumes into typed domain values; `tools/` remains outside the product import graph.
2. **Effective capability state needs two distinct unavailable classes**: absent compiled support or an admin-disabled flag is `disabled`; a compiled, policy-enabled capability with a missing provider or unhealthy dependency is `degraded`. Provisional entries stay `provisional` before runtime gates, and deliberate non-targets are omitted entirely, so neither can accidentally become `available`.
3. **T8 deployment probes are a useful but intentionally partial health adapter**: database+schema map to `postgresql`, blob maps to `blob_store`, and execution prerequisites map to `bubblewrap`. Unknown dependencies fail closed as unhealthy until their owning task supplies a real health source.

## MOD-01 (2026-09-20)

1. **`case Failed(failure)` on a dataclass captures the FIRST POSITIONAL FIELD, not the object**: for the frozen dataclass `Failed(seq, code, retryable, ...)`, the class pattern `Failed(failure)` binds `seq` (an int) — the guard `failure.retryable` only explodes at runtime (`'int' object has no attribute 'retryable'`), which mypy and ruff both pass. Whole-object capture is `case Failed() as failure`. Smoke (not types) caught it.
2. **The refcount must be reserved UNDER the GPU gate, before the slow process start**: the prior draft held the `anyio.Condition` lock across `controller.start(spec)` — a minutes-long media boot then (a) blocked interactive `load()`s behind the media start, and (b) stalled every `unload()` (they need the condition lock to `notify_all`), so the media load's own preconditions could never release. Correct shape: gate = conflict check + eviction + `references += 1` (fast, atomic), then the start outside the gate, serialized per-model by the model lock with a double-checked `running()` (single flight).
3. **A retry loop's `break` (retry taken) and its "stream ended without a terminal event" fall-through are two different paths**: after `break`-ing a retryable `Failed` mid-stream, control reaches the post-`async-for` block that synthesizes a TRANSPORT defect and consumes a SECOND backoff slot — the fake-timeout smoke showed 2 provider calls + 2 sleeps where 3 calls were the bound. A `retried` flag (continue on break; synthesize only on clean exhaustion) makes the budget honest.
4. **`ALLOWED_ADAPTER_IMPORTS` in tests/architecture/composition/ver-arch-03-001.py is policy data**: adding an authorized dependency (httpx/pydantic/anyio for MOD-01) requires extending that set in the same change (FND-07 nacl precedent) or the architecture suite fails on the import scan even though REFERENCE-DEPENDENCIES.md authorizes the dep.
5. **Preemption needs the adapter's transition() to release the lease on re-enqueue**: moving a leased/running job to `waiting_capacity` (or `queued`) while keeping `lease_owner` set violates `ck_jobs_lease_consistency`; `PgJobRepository.transition` now NULLs lease fields exactly when the target is a pending state or queued. `InteractivePreemptor` = checkpoint via `record_progress` + `transition(WAITING_CAPACITY, lease_owner=None, lease_expires_at=None)` — ch15 state machine only, no new states, `waiting_reason=capacity` persists the reason.
6. **A smoke driver that forgets `await` on an async section no-ops silently** (`RuntimeWarning: coroutine was never awaited`) and the remaining checks still "pass" — the live-llama.cpp section ran 13s shorter on the first pass for exactly this reason. Assert the section's side effects, not just its exit.

## Process (2026-09-20)

1. **TWO spellings of the project name EXISTED in paths and transposing them cost hours** (trap eliminated 2026-09-20): the repo directory is `milpbookLM` (…LM), but the plan file, notepad dir, and evidence filenames were `milpbookml` (…ml): `.omo/plans/milpbookml-implementation.md`, `.omo/notepads/milpbookml-implementation/`, `.omo/evidence/task-N-milpbookml-implementation.json`. A transposed `milpbooklm` path failed with ENOENT on every open (ls of the parent still showed the right name and could read it — the wrong bytes were in YOUR command, not the filesystem). All in-repo artifact paths (plan, drafts, notepad dir, evidence) were unified to the canonical `milpbooklm` spelling on 2026-09-20; the repo directory stays `milpbookLM` and the guide dir stays `milpbookml-implementation-guide/`. Lesson kept: when a file "mysteriously" cannot be opened, glob-expand the parent (`cat .omo/plans/milp*`) and diff the bytes before suspecting the FS.

---

## T11 (2026-09-20)

1. **`tools/spec/emit_conformance.py` needed no patch**: it already points at `packages/contracts/src/milpbooklm_contracts/capabilities.yaml` (moved in task 9), and its designed output path is `artifacts/conformance.json` (repo root artifacts, NOT `artifacts/verification/`). Honesty is by construction when the registry seed has 0 `implemented: true` entries: effective/advertised both come out `[]`, so the gate's "zero advertised-but-unimplemented" MUST NOT cannot be violated by hand-editing the emitter.
2. **The PHASE-GATE-001 acceptance suite does not exist in wave 1** — `tests/meta/` has only `normative_extraction/` and `capabilities/` groups; `PHASE-GATE` appears in the registry's `required_test_groups` (a test-group NAME in data, not a file) but no test implements it. Under D12 (no new test files), a gate task whose acceptance criterion is "PHASE-GATE-001 suite green" must be reported PARTIAL with the suite recorded deferred — a green verdict would be a fabrication.
3. **Gate-report ground truth lives in `.omo/start-work/ledger.jsonl`** (task-completed events carry commits/commands/deferred/verdict per task) joined with the PLANNING-HANDOFF "Phase entry and exit gates" table (Phase 0 required exit = 8 named items; map each to a workstream, and report each as satisfied/deferred). The registry's `required_test_groups` listing PHASE-GATE-001/CAP-PROFILE-001 is forward-looking data, not evidence.

## Spelling-unification (2026-09-20)

- Renamed `.omo/plans/`, `.omo/drafts/`, `.omo/notepads/`, and `.omo/evidence/task-*-…` from `milpbookml-implementation` to `milpbooklm-implementation`; live references updated in the plan text, journal, ledger, boulder.json (unstaged runtime state), and the notepad headers.
- Deliberately untouched: repo dir `milpbookLM`, guide dir `milpbookml-implementation-guide/`, and historical quotes (review round ids, evidence logs, the Process entry above).
- Case-sensitive sweep confirms: every remaining `milpbookml` hit in `.omo/` is a deliberate historical quote — zero live path references.

## T12 (2026-09-20)

1. **FastAPI `UploadFile` is not quarantine-safe by construction**: Starlette's multipart form parser may spool a large part to the process temp directory before the route receives it. ING-01a therefore parses the single multipart `file` part directly from `Request.stream()` without writing, retains only a boundary-sized tail, and sends only part bytes to `blob_root/quarantine`; filename, extension, and part MIME are never consulted.
2. **The existing schema has no acquisition-key column**: for immutable upload/paste snapshots the stable source origin includes `origin_kind + importer_version + content_sha256`, while the FND-05 job key is `source_id + content_sha256 + importer_version`. A per-notebook advisory lock makes the source/version lookup+insert concurrency-idempotent; another notebook gets separate ownership rows but reuses the global content-addressed blob.
3. **`source_versions.status='activating'` is the correct T12 terminal state**: T12 stops at quarantined+identified and reports that acquisition status while the durable acquisition job itself is `succeeded`. Setting the source version `active` or `sources.current_version_id` here would steal T14's parse/index-complete activation transaction.
4. **Removal is not purge**: the FND-03 source availability constraint already models reversible removal as `deleted_tombstoned`; select restores `active`. T24 owns purge closure, so its preview/confirm routes return the explicit stable `not_implemented_until_ing_02d` response rather than silently disappearing or deleting bytes early.
5. **Inline completion exposed a latent `PgJobRepository.transition` persistence gap**: the port accepts transition field updates, but the adapter previously packed lease/start fields only into JSON while leaving their first-class columns unchanged. Persisting `lease_owner`, `lease_expires_at`, and `started_at` is required for the legal queued→leased→running→succeeded acquisition-job path and the `ck_jobs_lease_consistency` constraint.
6. **Do not fabricate restriction rows for unrestricted local content**: `source_restrictions` is a deny/reuse/export policy table, not source ownership. Local unrestricted imports correctly create zero restriction rows; notebook membership scopes each source, while blob dedup alone crosses notebook/owner boundaries.

## T13 (2026-09-21)

1. **Terminal single-newline paragraph input needs an end-whitespace lookahead**: the initial text paragraph regex stopped before a final paragraph when it was followed by exactly one newline. `(?=\\n\\s*\\n|\\s*\\Z)` keeps structural paragraph IDs stable and retains terminal content.
2. **Parser imports must occur after the child network guard is installed**: importing the PDF module at child module load bypassed the import guard. The rootless `-I` child now installs its deny finder before importing pypdf/pdfplumber modules.
3. **The supplied T12 scratch PostgreSQL endpoint can disappear between dispatches**: `/tmp/opencode/t12-qa/.s.PGSQL.29519` was absent during T13 manual QA. Record real isolated-parser output and distinguish unavailable persistence QA from verified parser behavior.

4. **Live-metadata `create_all` composes dangerously with raw incremental migrations**: a fresh baseline can already contain newer columns, so every later raw `ALTER` must be idempotent. Use `IF EXISTS`/`IF NOT EXISTS` and drop-before-add constraint replacement to make fresh installs and interrupted-upgrade retries converge.

5. **A synchronously driven request job and a polling worker are two writers by construction**: enqueuing `acquire_identify` and immediately leasing/completing it from the API races the normal worker claim CAS. Synchronous acquisition is audit-only; enqueue only the parse job, and make worker claims filter to registered handler kinds so unhandled work remains queued rather than leased and failed.
## T14 (2026-09-21)

1. **Partial-unique-index swap order is load-bearing INSIDE one transaction**: promoting the new `active` source version BEFORE demoting the prior one is rejected by `uq_source_versions_one_active` (UniqueViolation) because PostgreSQL's uniqueness check sees the transaction's own un-demoted rows. Empirically proven both ways on the scratch (probe scripts in /home/srcds/dev/t14-smoke/). Demote-then-promote is the only order that commits; the canonical_documents side of the WIP already had the right order, the source_versions side did not.
2. **Running `tests/architecture` + `tests/meta` rewrites EVERY `artifacts/verification/VER-*.json`** (each VER test re-records its evidence JSON): a gate run dirties ~23 artifact files that must be `git checkout -- artifacts/verification/`'d before staging, or the task commits a wall of timestamp churn. This is why T14's prior WIP carried a stale VER-ARCH-03-001.json diff.
3. **The rtk shell wrapper kills `setsid cmd > log 2>&1 &` background launches** (FND-05 re-confirmed): the reliable shape is a launcher script that self-daemonizes with `nohup ... > log 2>&1 &` INSIDE the script, invoked in the foreground; `setsid bash x.sh &` through rtk hangs the tool call until timeout and starts nothing.
4. **Migration idempotency via the shared live MetaData**: 0003 does `METADATA.create_all(checkfirst=True)` + drop-before-create trigger re-apply + idempotent grant DO-block — no raw ALTERs. Fresh DBs (0001 already built provenance_edges from live metadata) and upgraded DBs converge to the same state; proven on both (fresh: 0001->0002->0003 + no-op second run; upgraded: T13 scratch 0002->0003).
5. **C901=10 bites closure-based FastAPI route builders**: two decorated handlers with authz branches in one `build_*_router` = complexity 11. Split into per-concern sub-router builders (`_build_guide_router`/`_build_activation_router`) mirroring source_lifecycle_routes.py; also the right Starlette registration shape (sub-routers first, static paths before `/{id}`).

## T15 (2026-09-21)

1. **SQLAlchemy `text()` does not extract `:param::cast`** — write `CAST(:param AS type)` explicitly; and reusing ONE named param for both a text column and a `regconfig` cast raises `AmbiguousParameter: regconfig versus text` — pass the value twice under two names (`:fts_config` / `:fts_config_rc`). Raw psycopg (scratch scripts) is the opposite dialect: `%(name)s`, never `:name`.
2. **Deterministic chunk IDs collide across generations**: `uuid5(CHUNK_NAMESPACE, node:chunker_revision:span)` repeats for every generation of the same version, so an `id`-alone PK breaks rebuilds (UniqueViolation on `index_chunks_pkey` before the composite unique can apply). PK must be composite `(index_generation_id, id)`; prev/next links lose their FKs (PG forbids FKs into non-unique columns) and are enforced by the chunker instead.
3. **Over-fetch retry loops must terminate on the COMPUTED cap** `min(cap, top_k*factor)`, not the raw cap: the last-iteration guard compared `limit == 500` while the effective cap for top_k=10 was 300, so every under-full search fell off the loop into `AssertionError` (silent 500s on all search modes).
4. **Failed generations resume in place**: `create()` resets `failed`→`building` (the atomic swap only promotes `building` rows), so a re-enqueued rebuild adds NO new generation row. Rebuild assertions must check the `{ready, superseded}` status set, not row counts.
5. **PG `simple` FTS config lowercases** (case-insensitive: lowercase `provenienz` hits `Provenienz` text); and a non-member search returns **404 "notebook not found"** (enumeration defense, source_http.py), 403 only for an insufficient member role.
6. **`configure_structured_logging` detaches ALL descendant handlers** of `milpbooklm_*` loggers at app startup — in-process log capture (e.g. asserting the post-hydration policy-deny WARNING) must attach its handler AFTER `create_app()`, not before.

## T16 (2026-09-21)

1. **A grounding manifest must freeze before retrieval, not at publication**: otherwise an activation swap can make the evidence set differ from the manifest the answer claims to pin. The retrieval command therefore accepts a frozen `source_version_ids` scope; the SQL admits inactive historical versions only when that explicit pinned scope is present, while ordinary retrieval remains active-version-only.
2. **Existing `messages.citations` and generation-manifest tables already support task-16 atomic publication**: no 0006 migration is needed. A single transaction validates every span first, then inserts only the assistant message and server-resolved citation payload; validator failure leaves no message row.
3. **Citation jumps must validate the node/version relationship, not only source-version visibility**: resolving the URL from an arbitrary node under an authorized source version would otherwise create a forged locator. The jump query joins source version, canonical document, and node before current authorization and purge checks.
4. **The supplied GPU chat endpoint was unavailable during final QA**: `GET http://127.0.0.1:8009/v1/models` repeatedly returned connection refused. The fake-provider smoke completed valid atomic publication, invented-ID rejection, cross-notebook rejection, purged unavailable state, and source-only abstention; the real-model smoke is recorded as infrastructure-blocked without restarting or reconfiguring the shared service.

## T17 (2026-09-21)

1. **FastAPI response unions need an explicit disabled response model**: annotating an endpoint as `StreamingResponse | JSONResponse` makes FastAPI attempt to build a Pydantic response field and abort app construction. Streaming command decorators must set `response_model=None`.
2. **`rowcount` is not a portable success signal for `INSERT ... SELECT`**: psycopg can report an unusable row count even when the ownership-filtered insert committed. `RETURNING messages.id` is both the atomic ownership result and the reliable inserted-message identity.
3. **Conversation reset is replacement, not deletion**: ARCH-05-005 keeps the old private history intact, marks its conversation `reset`, and returns a new open conversation carrying config/instructions but no messages. Deleting old messages silently destroys retained history and contradicts the domain invariant.
4. **SSE frame escapes are observable protocol bytes**: `"\\n"` emits a backslash and `n`; only `"\n"` produces an SSE line break. A real `TestClient.stream()` parser caught the difference before browser work began.
5. **Fresh migration runs exercise a different path from upgrades**: live SQLAlchemy metadata already creates the final composite `index_chunks` key, so 0005 must tolerate absent predecessor constraints. `DROP CONSTRAINT IF EXISTS` lets fresh installs and genuine 0004 upgrades converge before 0006 runs.

## T18 (2026-09-21)

1. **The capabilities registry is a frozen seed, not a runtime flag**: `tools/spec/check_capabilities.py::_check_seed_drift` requires `packages/contracts/.../capabilities.yaml` to equal the guide seed `milpbookml-implementation-guide/capabilities.generated.json` exactly; flipping `notebook_management` to implemented/enabled to make the UI create button live broke two meta tests and had to be reverted. The REST route works capability-independent; the UI affordance honestly renders disabled with the registry reason. Enabling a capability is a gate task, never a UI task.
2. **FastAPI rejects empty prefix + empty route path on include**: `APIRouter()` (no prefix) with `@router.get("")` raised `FastAPIError: Prefix and path cannot be both empty` once the sub-router was included. Every sub-router carrying collection routes must carry the full prefix itself; the composing wrapper stays prefix-free (same shape as source_lifecycle_routes.py).
3. **An SPA that never boots its session is logged out on every F5**: `AuthProvider` starting at `actor: null` with no mount-time `getActor()` means full reloads and deep links drop the user to the auth screen even with a live cookie. Boot `/auth/me` once on mount behind a `checking` flag and render the loading state while it resolves; deep-linking only becomes testable after that.
4. **CSRF applies to unsafe calls that carry the session cookie — even in-page `fetch`**: a same-origin in-page `fetch()` to `/auth/login` while the session cookie is live returns 403 (no `x-csrf-token`), while a cookieless login passes. Browser-QA REST calls must capture `csrf_token` from the login response and present it; `Origin` is a forbidden fetch header, so same-origin requests rely on the Referer fallback in `_origin_ok`.
5. **Biome a11y rules push segmented controls to semantic `fieldset`**: `aria-label` on a bare `div` trips `useAriaPropsSupportedByRole`; adding `role="group"` then trips `useSemanticElements` demanding `<fieldset>`. Use `<fieldset className="segmented" aria-label=...>` and neutralize fieldset UA defaults in CSS (`padding/margin: 0; min-width: 0; border: 0` where the design wants no border). For `<audio>` without a transcript (no React `transcript` prop exists), a targeted `biome-ignore` with the real reason is the honest fix.
6. **Serving smoke through the web origin exercises the real trust chain**: driving register→login→CSRF→SSE entirely through `http://localhost:5173` (vite proxy, `changeOrigin: false`) proves proxy, origin check, cookie, and CSRF together; the smoke parsed `{capabilities: [{id, ...}]}` (a dict wrapper, items keyed by `id`) — assuming a bare list with a `capability` key silently reported `state=MISSING`.

## Storage convention (2026-09-21)

ALL agent scratch (drivers, fixtures, DB clusters, logs, screenshots) lives under `/home/srcds/dev/milpbookLM/scratch/` exclusively — nothing outside the repo. Scratch DB socket is `<repo>/scratch/t13-smoke`, port 29521. DSNs use `host=/home/srcds/dev/milpbookLM/scratch/t13-smoke`. The eight former top-level dirs (`t13-smoke`, `t14-atlas`, `t14-smoke`, `t15-atlas`, `t15-smoke`, `t16-smoke`, `t17-smoke`, `t18-smoke`) were moved into `scratch/` on 2026-09-21; the t13-smoke PostgreSQL (db `milpbooklm_t13`) survives the move and stays running as the shared QA database. Scratch is git-ignored — never commit it.

## Process (2026-09-21, post-compact)
1. **Verify session ownership BEFORE killing opencode host PIDs**: cross-check the `-s ses_*` argument against `.omo/run-continuation/*` (constantly-churning file = the LIVE main session) and against dispatched task session ids. I killed my own session host by mistaking a fresh post-compaction restart (17min elapsed, 87% CPU during codegraph re-index) for an orphaned dispatch. Atlas survived only via host respawn.
2. An aborted task() dispatch leaves NO session process (abort happens before spawn) — confirm orphan existence via scratch/ dirs and download artifacts before assuming one runs.
3. A wedged serena MCP server (`start-mcp-server --project-from-cwd`) can spin 95% CPU for 20h; `ps --sort=-%cpu` finds it; killing it costs only serena tools (respawnable), not the session.

## T19 (2026-09-21)

1. The live Swift endpoint exposes build identity in `system_fingerprint` (`b11380-969bce4d2`) and server-side prompt/TG timings in non-streaming responses. One discarded warmup plus 15 kept sequential calls stayed inside D16's light-probe ceiling; a separate SSE request supplied first-byte timing without touching the serving process.
2. Reasoning tokens share the completion budget in this production recipe. At `max_tokens=128`, the German unanswerability probe consumed the budget entirely in reasoning and emitted empty visible content while the English equivalent answered. Quality evidence must retain this as a failure; an empty visible answer can never be accepted as successful abstention.
3. Big Pickle reachability and usability are distinct: `/models` was public and advertised `big-pickle`, but direct chat returned HTTP 403 because the free tier is restricted to the OpenCode client. Muse Spark Standard was network-reachable but returned HTTP 401 without credentials. The honest operational state is therefore degraded local-only routing, not failed task acceptance.
4. Corpus v1 locks 20 German and 20 English judgments for each of embedding recall, answerability, contradictions, citation entailment, and chat quality. CPU bge-m3 produced 1024-dimensional vectors and recall@3 = 1.0 overall/DE/EN against the fixed 0.95/0.90/0.90 thresholds.

## T20 (2026-09-21)

1. **A full page reload drops the web client's in-memory CSRF token** (`client.ts` module-level `csrfToken` is set only by `login()`): after F5 or a deep-link load, the next unsafe action fails `403 csrf_rejected` until the next login. The app's own links navigate client-side (App.tsx pushState + popstate routing), so real citation→viewer journeys never hit it — but browser drivers MUST navigate SPA-style (`history.pushState` + `PopStateEvent("popstate")`), never `page.goto()`, once logged in. App-side recovery (re-issue CSRF on `/auth/me` boot) is a recorded wave-2 finding, not fixed (T20-F1).
2. **Vite dev + Playwright `waitUntil: "networkidle"` is unreliable** (HMR websocket); T18's proven drivers use the default `load`. And playwright for this repo resolves only via `createRequire("<repo>/apps/web/")` — a bare `import "playwright"` from repo root fails.
3. **Two same-accessible-name buttons break strict mode silently at click time**: AuthRoute's mode toggle and the submit button are both "Register" in register mode. Target `form button[type=submit]` (the submit carries `className="primary"`, toggles carry none).
4. **In-process rate limiters reset with the process they live in**: the register limiter (3/IP/hour) is SlidingWindowLimiter in API memory, so a task-owned API restart cleanly resets the budget mid-journey — but budget accounting must happen BEFORE launching multi-registration drivers (2 browsers = 2 registers).
5. **The registry's `required_test_groups` names (CAP-PROFILE-001/PHASE-GATE-001/PARITY-ACCEPTANCE-001) are forward-looking data, not suites** (T11 learning re-confirmed at gate time): phase-1 is judged by live REST/journey evidence recorded in the gate report, with the missing automated groups recorded as wave-2 deferrals — phase-0 gate precedent, advertised=0 stays emitter-pure in conformance.json.


## Process (2026-09-21, T20 verification)
1. Reboot recovery order that works: pg_ctl on scratch/t13-smoke/pgdata (socket -k scratch/t13-smoke, port 29521) → bge-m3 :8010 → API (start-api.sh) → worker (start-worker.sh) → vite (start-web.sh). :8009 is user-managed; do not spawn unless absent AND authorized.
2. A dying vite dev server leaves in-page /api proxy fetches hanging forever: the SPA sticks at `Loading view...` (AuthProvider `checking`) with 401s already answered in API logs. Symptom mimics an app hang — always curl the ports first.
3. Playwright E2E drivers must navigate SPA-style (pushState/click links), never full-page gotos: gotos wipe in-memory CSRF state (T20-F1) and re-trigger the auth boot.

## T21 (2026-09-21)

1. **python-pptx drags the stdlib network chain at import time**: `pptx/shapes/autoshape.py` imports `xml.sax.saxutils`, whose module-level imports pull `urllib.request → http.client → socket → ssl`. Under the parser child's import finder this crashed PPTX parsing as `internal ImportError`. Fix pattern: preload the inert chain (`importlib.import_module("xml.sax.saxutils")`) BEFORE installing the finder, then REPLACE the five blocked modules in `sys.modules` with stub modules whose attribute access raises — the no-network invariant holds for actual use while python-pptx still imports.
2. **The structural chunker never chunked TABLE containers**: `_walk_units` checked `kind is NodeKind.TABLE` only AFTER `text = node.text or ""; if not text.strip(): return`, so any table node (a container with text=None) returned early and indexed zero chunks. Invisible for text/PDF sources (paragraph text only); exposed the moment CSV/XLSX canonical documents hit the indexer. Fixed by moving the TABLE branch before the early return.
3. **Quarantine text validation must be scoped to the text family**: routing `_identify` through the shared sniffer initially left `elif _text_rejected(path)` running for XLSX/DOCX/PPTX too — office zip bytes failed strict UTF-8 and were rejected at acquisition with 422 unsupported. Scope byte-level validation to IdentifiedMedia.{TEXT,MARKDOWN,CSV}.
4. **A module named after its library (parsers/pptx.py importing `pptx`) trips basedpyright's implicit-relative-import heuristic** and is a reader footgun even though Python 3 absolute-import semantics keep runtime correct (proven by child smokes). Renamed to `docx_parser.py`/`pptx_parser.py` matching `csv_parser.py`.
5. **httpx over plain http silently drops the Secure mb_session cookie from the jar** (same class as the TestClient finding in FND-04): API-level QA drivers must present `Cookie: mb_session=...` explicitly and use the API's configured trust origin (`http://localhost:5173`, not `127.0.0.1:8000`) as the Origin header or CSRF rejects with `origin_rejected`.
6. **list-invariance vs JsonValue**: mypy strict rejects `extra["header"] = some_list[str]` (list invariance); `list[JsonValue](values)` satisfies both mypy and ruff (which flags the `[x for x in y]` workaround as C416).

## Process (2026-09-21, T21 verification)
1. pidfile kills can miss daemonized children: start-api.sh pidfile records the nohup/setsid wrapper; uvicorn survives as an orphan still holding :8000 (stale rate-limiter state persisted across a fake restart). To REALLY restart a listener: find the port PID via `ss -tlnp | grep ":PORT "` and kill that.
2. Register/login limiters are per-client-IP in-memory sliding windows (`register:{ip}`, `login:{ip+account}`) - a busy QA evening exhausts them; real API restart resets.
3. API surface: uploads POST /api/v1/sources/import?notebook_id=... (router prefix /api/v1/sources, notebook_id is a QUERY param); jobs GET /api/v1/jobs/{id}; canonical gate dirs (tests/meta|architecture|unit) miss tests/fixtures suites - run both.

## T22 (2026-09-22)

1. **Validate numeric IP literals inside the connector too**: URL prevalidation is not enough; every actual `connect_tcp` call must classify literal hosts before dialing, or direct metadata/private literals bypass DNS-candidate checks. The pinned httpcore backend now validates literals and all hostname candidates before handing a numeric address to the real connector.
2. **A service-owned HTTP client must outlive individual fetches**: closing a per-fetch client also closes its shared transport/pool. The hardened service owns one client and exposes `aclose()` for composition shutdown; tests close it in `finally`.
3. **A wall-clock deadline must include streamed body consumption**: wrapping only `AsyncClient.send(..., stream=True)` caps headers, not a slow-trickle body. Keep `_read_capped()` and response closure inside the same AnyIO deadline scope; a delayed-stream regression test proves red-to-green.
4. **Selectolax node wrappers are not identity-stable**: ancestry comparisons must use `Node.mem_id`, not Python `is`, because traversals can return distinct wrappers for the same underlying node.
5. **Job payload parameters persist under `payload.params`**: web capture provenance (final URL, timestamp, headers, redirect chain, hash) must be read and propagated from that nested structure into parser locator context.
6. **The configured basedpyright process does not resolve this uv workspace/venv**: it reports broad pre-existing missing workspace imports and an older Python target, while project `uv run mypy`, Ruff, runtime tests, and focused LSP files are clean. Do not mutate product imports to appease that external environment mismatch.

## T23 (2026-09-22)

1. **The OCR child inherits the WORKER process env**: `parsers/image.py` reads `TESSDATA_PREFIX` from `os.environ`; system tessdata ships eng+osd only, so a worker launched without `TESSDATA_PREFIX=scratch/t23-ocr/tessdata` fails every deu OCR with "Error opening data file". `start-worker.sh` does not set it — export it in the launching shell (it propagates through `nohup setsid`) and verify via `/proc/<worker-pid>/environ` before any OCR QA.
2. **Protocol/adapter annotation parity is mypy-enforced at the composition site**: extending `jump()` to expose bbox widened `PgGroundingStore.jump` to `dict[str, str | int | tuple[float, ...]]`; the `GroundingStore` protocol in application kept the old `dict[str, str | int]` return, which mypy rejects (dict invariance + protocol conformance) at `composition.py`. Bbox is `tuple[float, ...]` in the contract (`SourceLocator.bbox`), so int-tuple annotations also fail on the comprehension item type. Widen protocol + adapter + local in one pass.
3. **A new third-party import in adapters breaks the ARCH allowlist gate independently of pyproject**: adding Pillow (already in the tree via pdfplumber) still required adding `"PIL"` to `ALLOWED_ADAPTER_IMPORTS` in `tests/architecture/composition/ver-arch-03-001.py` — the architecture test is the source of truth for the import policy, not the dependency manifest.
4. **pidfiles lie about daemonized listeners** (re-confirmed for the worker): `worker.pid` recorded a dead wrapper PID while the real worker (`uv run` parent + python child, worker-id `t22-w1`) ran on for hours, and the API uvicorn outlived `api.pid` too. Find the true PIDs via `ss -tlnp` (API) / `ps -eo pid,args | grep milpbooklm_workers` (worker), compare process start time to HEAD, kill explicit PIDs, then relaunch via the `scratch/t20-gate/start-*.sh` scripts.
5. **The activate route does not return the index job id** (`source_payload(view)` without `job_id`), so drivers cannot poll it — prove indexing by the downstream search hit (rank-1 with embedded text) rather than by job state.

## T24 (2026-09-22)

1. **Unchanged remote bytes still need a new refresh snapshot**: returning the prior `SourceVersion` reused its parse idempotency key with a new capture timestamp and correctly raised `IdempotencyConflictError`. Explicit refresh now always creates N+1; only activation moves the retrieval pointer.
2. **Purge needs a trigger-scoped privacy path**: immutable canonical/manifests/artifact rows correctly reject ordinary deletes, so the worker sets a transaction-local purge context recognized by the guards. The API transaction only tombstones access and creates the task; controlled-copy erasure remains worker-side.
3. **Blob erasure is two-phase**: after removing identity references, mark unreferenced blobs `purged` and commit, unlink filesystem copies, then delete bookkeeping and complete the report. Deleting only DB rows leaves orphan files; deleting files before blocking reads creates missing-referenced integrity incidents.
4. **The live refresh journey exposed a real same-content bug that unit/type gates missed**; driving N -> N+1 through the hardened fetcher, worker parse, activation and purge is mandatory evidence for lifecycle work.

## T25 (2026-09-22)

1. **Three coordinate frames met at the citation seam and only a live heterogeneous journey caught it**: the chunker emits node-relative spans [0, len(unit_text)]; text/PDF parsers persist node-relative char extents; markdown persisted DOCUMENT-relative extents; and every other phase-2 family persists structural locators (block/slide/sheet/bbox/dom) with NO char extents. T16's publication validator demanded a char-covering locator row, so any citation into any phase-2 family raised `evidence has no valid canonical locator` — invisible to all unit suites (they never cross the real chunk->cite->validate seam). Fix: validator accepts extent-less structural locators via node identity+authz (char-typed still need coverage); markdown now emits node-relative extents. E2E-002 full-heterogeneous is the only instrument that proves this seam.
2. **Commit timestamps lie about process freshness; file mtimes do not**: the T24-era API/worker both predated the last write to pg_purge.py (mtime 15:50:49 vs process starts 15:32/15:41) even though HEAD's code commit was 15:51 — the working-tree files kept changing after the processes launched. Before any live drive, compare product-file mtimes (not git log) against process start times; restart on the newer-file side.
3. **generate_fixtures.py rewrites every zip-based binary (embedded timestamps)**: regenerating goldens churns docx/pptx/xlsx/hostile-office bytes with identical content. After a targeted golden regen, `git checkout --` the untouched binaries and keep only the intended golden JSON diff.
4. **The fixtures VER record lags the suite**: VER-INGEST-GOLDEN-001.json committed at T24 omitted golden-text/pdf from golden_families even though that suite added them; a gate-run re-record is the honest refresh (keep it when the content diff is real, revert pure timestamp churn on the other VER files).

## T26 (2026-09-22)

1. **frozen+slots dataclass exceptions are unusable inside anyio deadline scopes**: `contextlib` re-assigns `exc.__traceback__` in pure Python while unwinding `@contextmanager`-based scopes (`anyio.fail_after`/`fail_at`), and the dataclass-generated frozen `__setattr__` either raises `FrozenInstanceError` (no slots) or calls `super()` against the pre-rebuild class and TypeErrors (slots). PROVEN live: T22's `HardenedFetchService.fetch` turned an upstream 503 into `TypeError: super(type, obj)...` instead of the typed refusal — invisible to its suite because no test raised a refusal inside the deadline scope. Fix: hand-written typed exceptions (`__slots__` + typed `__init__` + `super().__init__(code, detail)` so pickling reconstructs enums). `ProviderFailureError`/`AcquisitionError` carry the same latent shape (deferred to their owners).
2. **A user-defined `__setattr__` cannot rescue the shape**: with `frozen=True, slots=True` the dataclass machinery hard-errors "Cannot overwrite attribute __setattr__"; without frozen the generated setattr blocks every write. Don't fight it — write exception classes by hand.
3. **SearXNG verified facts to keep encoded**: `format=json` works ONLY when `json` is in `search.formats` (non-default; a misconfigured instance answers 403 with an HTML page — surface as `http_status`, and a 200-HTML as `unexpected_content_type`); the built-in limiter requires Valkey (limiter off + no Valkey service per D7); `unresponsive_hosts` in the JSON payload is the per-search engine-degradation signal; `/config` carries the engine list for health observability. Adapter default config revision must be bumped in lockstep with `infra/podman/versions.lock`.
4. **In-process pure-ASGI fake beats a port-level fake for HTTP adapters**: driving the REAL adapter through `httpx.ASGITransport(app=fake)` exercises content-type validation, deadlines (fake `anyio.sleep` gets genuinely cancelled), the format gate, and wire params (`fake.requests` records the query string) with zero framework deps and zero network — the models-package FakeProvider pattern lifted one level down the stack.

## T27 (2026-09-22)

1. **The baseline ch05 research tables already existed with a narrower status vocabulary**: research_runs/run_steps/evidence_snapshots were created by 0001's live-metadata create_all, and the evidence-immutability trigger already guarded snapshots (including one-time promotion). RSR-01b is therefore a schema EXTENSION (mode/budget/tools/approved_tools/revision, step status/input_manifest/tool_name, status checks with created+paused), not new tables - and the plan's "Migration 0007" was already taken by ing_02d, so the real number is 0008.
2. **A schema-convergence probe must not share search_path with the tables it probes**: `SET search_path TO probe, public` made `create_all(checkfirst=True)` see the EXISTING public tables through the fallthrough and create nothing, turning the "fresh path" into a vacuous no-op proof (the 0008 statements silently re-applied to the shared tables - harmless only because they were already converged). Real proof needs a throwaway DATABASE (created via the cluster's OS-user superuser over the socket, bootstrapped with pgvector + the shared DB's uuidv7 definition, granted CREATE on public for PG17+), then 0001->0008 and a column+constraint-definition diff against the upgraded DB.
3. **Field-update dicts crossing a store port must speak the VIEW's vocabulary, not the TABLE's**: passing `candidate_imports`/`plan` (run-table JSONB columns) as `field_updates` into a CAS whose memory double did `dataclass.replace(view, **updates)` exploded with "unexpected keyword argument"; the closed vocabulary belongs on the view dataclass (add the fields), and timestamps must cross as datetime objects, never isoformat strings.
4. **Slots dataclasses have no `__dict__`**: test doubles that need to "update" frozen+slots domain objects must use `dataclasses.replace`, not dict-spawned reconstruction.
5. **An exhausted scripted planner that auto-finishes will terminate runs the test meant to leave mid-flight**: the planner double needs call-counting `should_stop` boundaries (or callable script entries evaluated at pop time) to cut execution at a specific iteration; otherwise drive 1 silently publishes SUCCEEDED and every later "resume" is a no-op that looks like a bug.
6. **Tool summaries must be persisted on the step row, not only kept in the executor's session memory**: without `tool_result={"summary": ...}` + evidence linkage written by `finish_step`, a resumed run rebuilds planner turns with no evidence references and would re-run side effects (exactly what ARCH-11-010 forbids) - caught by the pause/resume unit test asserting single-import.
7. **Unknown-tool proposals deny at the PARSE boundary (malformed_action), not the authz table**: a "call browser.eval" injection never reaches `authorize_tool_call` because parse_action refuses the tool name first; the injection-log evidence therefore splits across two refusal reasons (parse for unknown shapes, authz for known-but-unapproved tools), and both belong in the audited corpus.
8. **httpx.ASGITransport requires the ASGI app's `__call__` parameters to be WIDER than dict-typed scopes** (contravariance: MutableMapping-accepting callables, not dict-accepting): typing the fakes with ASGI scope/receive/send aliases fixed the composition wiring without casts, and widened T26's FakeSearxng annotations for free.
9. **Per-job `asyncio.run` needs per-job async clients**: the worker handler builds fresh SearxngSearchService/HardenedFetchService inside each research.execute job (one event loop per job, closed in finally) so pooled connections never cross dead loops; the worker CLI's fake-web mode (default when no SearXNG URL is configured) injects the deterministic fakes via ASGITransport, mirroring the API's chat_provider=fake precedent.

## T28 (2026-09-22)

1. **Playwright's DEFAULT Chromium launch silently injects `--no-sandbox`** (`chromiumSandbox` defaults to false): a /proc cmdline scan of a default `chromium.launch({headless:true})` shows `--no-sandbox` on the renderer with `Seccomp=0`. The sandboxed launch needs BOTH `{ chromiumSandbox: true, channel: "chromium" }` — the channel matters too: default `headless:true` uses the `chrome-headless-shell` binary, not the full version-matched Chromium. Posture proof that survives Chromium's argv rewriting: `NoNewPrivs=1 + Seccomp=2` on renderers (`--type=` flags are CONSUMED by the rewritten cmdline, so type-based matching finds nothing).
2. **Main-frame downloads never fire the context "download" event** in Playwright 1.58/Chromium 145: navigating `page.goto()` to a download URL rejects with "Download is starting" but `context.on("download")` stays silent for 9+ seconds. The working capture is a CONCURRENT `page.waitForEvent("download")` raced against goto (Promise.all pattern). And never `await` that waiter on the success path — its timeout is paid on every normal navigation (20s/page; our suite went 217s → 16s after unawaiting it).
3. **JS-render fixtures must compose their markers at runtime** (`parts.join('')`): a marker literal inside the inline `<script>` source IS static content — a static httpx fetch "sees" it and the insufficiency proof collapses. Also: `<system>` tags in HTML are consumed by the parser; assert on the rendered sentence, not the raw payload constant.
4. **Worker session teardown must reap the Node subprocess deterministically**: anyio's `Process` has `returncode` (NOT `poll()`), and `browser.process()` does not exist in Playwright 1.58 — locate the browser tree by walking /proc PPIDs under the worker's own pid (the kernel-flag probe reads /proc/<pid>/status per descendant).
5. **The automation gate needs the failed-fetch URL to survive resume**: turn summaries are rebuilt from `research_run_steps.tool_result` (T27 lesson 6 sibling) — writing `tool_result={"summary": f"fetch failed for {url}: …"}` on the failed step is what makes `browser_automation_denial` matchable both mid-run and after resume.

## T29 (2026-09-22)

1. **cgroup v2 delegation facts beat the literal docs**: `user.slice/user-<uid>.slice/cgroup.subtree_control` is root-owned (looks undelegated), but systemd delegates at `user@.service` — discovery must WALK from `/proc/self/cgroup` upward and PROBE (mkdir + memory.max write + rmdir), never trust one file's writability. And `cgroup.subtree_control` writes on this kernel reject the combined "+cpu +memory +pids" form with EINVAL — one controller per write.
2. **`memory.max` without `memory.swap.max=0` is a soft ceiling**: tmpfs overflow (and anon under pressure) silently swaps out — a 24M cgroup happily "wrote" 64M to tmpfs with `memory.peak == memory.max` and zero OOM events. The hard boundary needs the swap valve closed per-execution. Proven by probe: swap-off flips the same run to exit 137 + `oom_kill 1`.
3. **Post-`Popen` cgroup attach races bwrap's payload fork**: moving only the supervisor pid leaves the actual sandboxed child in the outer cgroup (limits unenforced, invisible to tests that only check `kill_method`). The race-free form is `preexec_fn` self-join — the child moves itself between fork and exec, so the payload inherits limits from its first instruction.
4. **bwrap `--size` applies FORWARD to the next `--tmpfs`** (`--size N --tmpfs /tmp`), and 0.11 hard-errors when `--size` is followed by anything else — `--tmpfs /tmp --size N --tmpfs /workspace` silently quotas the WRONG mount (workspace) and leaves /tmp at the default half-RAM.
5. **Recorded host facts can be wrong; re-verify before encoding them**: the "seccomp EINVAL-unsupported" fact did not hold under the real launch path — bwrap with `--seccomp <fd>` + NoNewPrivs installs a filter fine (`Seccomp: 2`). The honest posture stayed "no filter applied" (none curated) but for the TRUE reason; a docstring asserting the false reason is a latent audit poison. `ruff format` drift across ~95 pre-existing files also means format --check was never an enforced gate — only `ruff check` is; format just the files you own.

## User fix: capability surface + health constants (2026-09-22)

1. **A day-one placeholder boolean is invisible to every gate**: the registry's all-false `implemented` flags survived because check_capabilities only pins ids/classifications vs the frozen ch02 table — booleans are deliberately unpinned (registry-vs-runtime honesty), so "implemented" must be flipped by hand in BOTH the YAML and the seed JSON (checker enforces parsed equality); flip `implementation_status` too or the registry self-contradicts (ver-arch-00-049 pins effective⇒implemented semantics).
2. **Derive deployment constants from their owning artifact, never pin them**: `EXPECTED_SCHEMA_REVISION="0001_baseline"` staled through 7 migrations silently; the fix is `ScriptDirectory.get_current_head()` exposed next to the migration harness (single source of truth) and an INDEPENDENT regex-graph guard test that fails the moment the derivation lags the versions dir. Note this venv's alembic (1.20.0) has `get_current_head` but NOT `get_current_heads` — probe the installed API before writing the call.
3. **Provider dependency ids ≠ provider names**: the registry gates on `chat_provider`/`embedding_provider` while config speaks `llama_cpp`/embedding-base-url; map wired reality onto dependency ids in composition (chat always wired, embedding iff base_url set) instead of hoping an env CSV carries registry-internal id strings. Feature FLAGS remain a deliberate admin-policy gate — they cannot be derived, only env-set (MILPBOOKLM_ENABLED_CAPABILITY_FLAGS).
4. **conformance.json's `effective` uses an empty policy by design** (emit_conformance skeleton), so registry flips never change it and the meta tests stay green — but its `enabled` field (implemented&&enabled) IS registry-derived; CI regenerates it, local runs read the checked-in copy.

## 2026-09-22 — Global session-invalidation handling (web)
- ky's shared instance is the single choke point for auth rejection: an `afterResponse` hook can emit a module-level `onSessionInvalidated` signal while returning `undefined` (pass-through), so per-mutation error handling stays intact — no interceptor rewrite needed.
- 403 bodies are NOT a reliable session-death signal by status alone: policy authz denials (`deny:*` from `PolicyReason`) reuse the exact `{"detail":{"reason":...}}` shape that `CsrfOriginMiddleware` uses for `origin_rejected`/`csrf_rejected`. Trigger set must be an exact reason allowlist, else "not your notebook" 403s would log users out.
- No DOM test env in the workspace (no jsdom/happy-dom, downloads banned): node-env vitest CAN drive the real ky pipeline by stubbing `globalThis.Request` (node's Request rejects ky's relative `prefixUrl` URLs — a ~15-line FakeRequest with `headers`/`clone()` suffices) and stubbing `fetch` per test. React-provider reaction stays covered at the client-contract seam instead.
- Loop-safety lives in the listener, not the emitter: the client fires per credential failure (faithful), AuthProvider guards via an `actorRef` synced by `applyActor` so repeat signals (e.g. wrong-password 401s on the login screen) never re-clear.
- Focus revalidation must distinguish 401 (clear session) from transient network failure (keep session); the react-query `refetchOnWindowFocus` equivalent for the hand-rolled provider is one `visibilitychange` listener gated on `document.visibilityState === "visible"` + actor presence.
- `pnpm lint` (biome) is red on HEAD (`useLiteralKeys` in NotebookRoute/ViewerRoute/vite.config, pre-existing); gate is "no NEW diagnostics from changed files", verified with scoped `biome check`.
- `signOut` should clear local session state even when the logout POST itself 401s (session already dead server-side) — `await logout().catch(() => undefined)` before clearing prevents the stuck-state bug class from the logout path too.

## 2026-09-22 — Configurable auth rate limits

- A short configured browser-QA window proves the limiter's real all-attempt semantics without changing production defaults: two successful logins plus one wrong password consumed a three-attempt budget, the fourth request returned 429, and waiting `Retry-After + 1` restored a successful login.
- Derive the `Retry-After` header and JSON retry value from the same computed wait so browser guidance and machine-readable behavior cannot disagree.

## 2026-09-22 — Auth rate-limit WIP completion (this session)

- The in-memory per-IP limiter makes localhost ONE bucket shared by the human and browser tests — lockouts during QA are self-inflicted, not a code bug. Dev-stack fix belongs in `scratch/t20-gate/start-api.sh` env knobs (`MILPBOOKLM_LOGIN_MAX_ATTEMPTS=50`), never in `InstallationConfig` defaults.
- `SlidingWindowLimiter.allow()` must NOT append on denial; `retry_after_seconds(key)` then reads the oldest in-window event for an exact ceil hint — a frozen `FakeClock` + `.advance()` pins both the 429 body and the `Retry-After` header deterministically.
- 429 must stay off the web client's session-invalidation trigger set (`isCredentialFailure` = 401 or 403-with-credential-reason only): a rate-limited user still has a valid session; logging them out would compound the lockout.
- `authFailureMessage` returning a constant "Authentication failed" for everything non-401/429 beats surfacing `error.message`: ky's HTTPError message embeds the request URL, which leaks internals into the UI.

## T30 (2026-09-23)

- Output publication has two different ownership domains: `OutputCollector.discard()` removes broker-owned quarantine copies only; finalized blob objects must survive. Always discard the original collected tuple in `finally`, including authorization refusal, after `publish()` has checked every output before its first blob write.
- Execution publication authorization must use the durable server-owned `JobRecord.actor_user_id` and `notebook_id`, never duplicate payload identities. Reject mismatched payload duplicates before staging.
- A lower-level real-bwrap path is not enough to advertise `code_data_analysis`: without a dedicated authenticated API that authorizes every input blob and a reviewed deployed runtime image/admin broker form, the capability must remain disabled even when worker integration tests are green.

## Full-story journey (2026-09-23)

- QA technique — capturing the in-memory CSRF token from a browser session: the planned workaround (hooking `window.fetch` before login to clone the login response) does NOT work for this client: ky@1.14.3 binds `globalThis.fetch` at `ky.create()` time (`fetch: options.fetch ?? globalThis.fetch.bind(globalThis)` in Ky.js), and that runs at module load — before any post-load `window.fetch` replacement can take effect. The reliable path is Playwright MCP's `network_request` tool: find the login index via `network_requests` (filter `auth/login`), then read `part: "response-body"` → `csrf_token` verbatim, and store it on `window.__qa_csrf` for in-page REST calls. General rule: for HTTP clients that capture fetch/XHR at construction, read captured response bodies from the network layer, not from in-page monkey-patches.
- QA technique — notebook search contract: `POST /api/v1/notebooks/{id}/search` with JSON `{query, mode, top_k, ...}` (GET → 405); like every POST it needs the `x-csrf-token` header (first round without it → 403 `csrf_rejected`). Search results reference `source_id`/`source_version_id` per hit, so activation→indexing can be proven by polling for the new ids (indexed within ~1 s of activation on the fake embedding path).
- Research runs are deterministic to a terminal state with the fake web: `source_discovery` + default budget (max_steps 64) finishes in <1 s as `failed/budget_steps_exceeded` (1 `web.search` + 1 `web.fetch` + 62 plan steps), job state `succeeded`, no source promotion. Distinguish the two ledgers in reports: job = execution correctness, run = goal outcome. Creation schema is `{notebook_id, goal, mode, tools, ...}` — `tools` must be exact `ResearchTool` names (7 values); there is no `topic` field.
- Product nits observed end-to-end: (a) chat citation chips duplicate the same source version+node per answer, and the citation list keys collide → React duplicate-key console warnings every answered turn; (b) chat conversation state is lost when SPA-navigating to a citation viewer and back (notebook unmount drops component state).

## Feedback batch + T30 close-out (2026-09-23)

- ORIGIN GATE gotcha: the API's `MILPBOOKLM_BASE_URL` is `http://localhost:5173`. The `CsrfOriginMiddleware` compares the request Origin/Referer origin to that string exactly — a browser session on `http://127.0.0.1:5173` gets `403 origin_rejected` on every unsafe method. Always drive the app from `http://localhost:5173` in Playwright.
- STALE-PROCESS gotcha: the dev API/worker are started without `--reload`. After any Python fix, the live verification runs the OLD code until `scratch/t20-gate/start-api.sh` + `start-worker.sh` are re-run. Symptom: fixed behavior still failing live with identical old signatures (e.g. 64-step loop after the F4 fix).
- The web client's session-invalidation path (403 → `logout()` POST) CANNOT complete when the in-memory CSRF token is wiped (full page reload, T20-F1): the logout POST itself needs CSRF, so the PG session stays live, the cookie stays in the browser, and the next login is blocked with `403 csrf_rejected` (login 401 = bad creds; 403 = live cookie present without valid CSRF). Recovery: in-page `fetch('/api/v1/auth/logout', {method:'POST', credentials:'include', headers:{'X-CSRF-Token': <captured>}})` → 204, then UI login.
- F4 fix (f3f67f6): `SourceDiscoveryPlanner` is tools-aware — after a successful fetch, if `source.import` ∉ `context.tools` it returns FINISH with an honest summary. Executor adds a strict-adjacency denial-loop guard: 2 consecutive identical `(tool, denial)` authz denials → run fails with typed `error_code="action_denied_loop"` (DENIAL_LOOP_THRESHOLD=2); any executed tool or parse failure resets the streak.
- F2 fix (bcfb5c4): route-external module store `apps/web/src/state/conversations.ts` (Map keyed by notebookId) seeds `useState(() => getActiveConversation(notebookId))`; the existing `enabled: conversationId !== null` query then refetches the server-authoritative conversation (schema embeds `messages`). Tab is remembered the same way. Scope: in-tab only — a fresh tab lands on "Start a private conversation" by design (consistent with in-memory CSRF).
- F5 fix (bcfb5c4): `citations.ts` — `citationKey(c) = source_version_id:node_id`, `dedupeCitations()` keeps first occurrence; chips keyed by the composite id.
- Brand: SVG mark = open evidence page (two light pages, violet spine) + 4-point citation spark, DESIGN.md tokens (#111419 tile, #f3f5f7 pages, #7775e7 accent); `public/favicon.svg` + header `BrandMark.tsx`.
- Live F4 verification: limited-tools run succeeded in 5 steps (plan,search,plan,fetch,plan); full-tools run succeeded search→fetch→import→finish and the import PROMOTED a real source version (visible in source_versions within ~1s, status parsed→active).
- Playwright MCP arg names (verified 2026-09-23): `fill_form` fields need `target` (not `ref`) + `element`; `click` needs `target` + `element`; `take_screenshot` needs `scale`; `network_requests` needs `static:false`; `network_request` takes `index` (1-based from network_requests output) + `part` ("response-body"); `look_at` takes `goal` + `file_path`.

## 2026-09-23 T31 STD-01 artifact framework (commit b22ebac)
- Verified first-hand by Atlas: 364 unit/meta/arch tests pass, ruff clean, mypy 218 files, lint-imports 4 contracts kept. Migration 0009_std_01 additive (widens ck_artifacts_status to 7 states), reversible, live DB at 0009.
- Browser D17 QA green (in-page fetch from logged-in page, CSRF from login 200 body): create 201 draft → generate 200 ready (v1, etag 0→3 = 3 CAS steps, manifest frozen, evidence pinned) → stale If-Match edit 409 artifact_state_conflict → matching edit 201 v2 (v1 sha byte-identical = immutability) → DB lineage confirmed (v2 manifest parent = v1 manifest) → export grant 200 / download 200 / path-mismatch 400 export_grant_mismatch → study state CAS 409 on stale If-Match, snapshot 201 private → capability artifact_lifecycle published as state "disabled" reason "admin_policy_disabled" (honest: implemented=true, enabled=false).
- CSRF/session recovery that worked: page reload wipes client-side CSRF token; session cookie is HttpOnly (JS cannot clear). Fix = revoke sessions in PG (`DELETE FROM sessions WHERE user_id=...`), click UI Log out (401 no-op clears client state), fresh UI login, capture csrf_token from login 200 body.
- Problem-title quirk (cosmetic, not a defect): source_http `problem()` emits generic title "Source acquisition rejected" for all typed 4xx problems; the machine-readable `code`/`type` fields are correct (artifact_state_conflict, study_state_conflict, export_grant_mismatch). UI must key off `code`, never `title`.
- Worker self-fix pattern worth keeping: `_GenerationPipeline._publish` originally didn't persist the version row; regression test asserts exactly one published version after generate.
- Artifact QA objects retained in live DB: artifact e3c61861-0d84-438f-bc46-2c20ff741517 (2 versions) in notebook 01a0cb45-... — fine to leave (real usage data).

## 2026-09-23 T32 STD-02a notes close-out

- Notes needed NO new migration: notes/note_revisions tables already landed with T31's db/tables/studio.py metadata (0009_std_01); T32 is pure application/adapter/API code over that schema. Check the studio.py metadata before assuming a task needs a migration file.
- Chat-context pinning shape that passed review: FrozenManifest carries note_contexts resolved AT FREEZE (membership-checked, duplicates rejected, unavailable rejected) — publication then RE-RUNS the same resolver and compares tuples, so a mid-flight membership revocation fails publication (note context is no longer authorized), same revalidation spirit as T31's export download recheck.
- Explicit-selection-only retrieval: the note path bypasses retrieval entirely (`prepare` returns trace "note-context-only" when sources are empty but notes are pinned) — abstain fires only when BOTH evidence and note contexts are empty; the "unselected note never appears as evidence" invariant is testable by asserting retrieval-trace + citation payload, not by absence queries.
- Replacing a demo stub with the real surface is a test-visibility seam: notebook_routes' /note-draft NOTE_MUTATE demo disappeared and test_api_auth's owner/viewer policy test now drives the real POST /api/v1/notebooks/{id}/notes (201/403 deny:role) — when a placeholder endpoint is superseded, port its policy assertions, don't delete them.
- Conversation SSE + typed store-unavailable errors: bare `RuntimeError("... is unavailable")` in route helpers is unreachable-by-mypy noise; a module-private `_ConversationDependencyUnavailableError` keeps the boundary typed. GroundingError now surfaces as 422 `grounding_invalid` (sync JSON) and an SSE `event: error` frame (stream) — mid-stream failure must terminate the generator, not raise into the ASGI send loop.
- Capability registry deliberately NOT flipped this task (notes stays not_started/enabled=false): the flip requires capabilities.yaml + capabilities.generated.json lockstep + conformance regen and belongs to the enablement/gate task (35/36), matching the plan's staging mandate; unit-level E2E-004 journey evidence stands on its own.
- Gate deltas recorded: pytest 364 → 380 (16 new: 1 route journey + 9 live-PG store + 6 transform-kind), mypy 218 → 226 files, ruff clean, lint-imports 4 kept — zero code edits needed at closeout; the interrupted worker's tree was already green.

## 2026-09-23 T32 STD-02a notes (commits b783761 + 9cbc3a1)
- Gates verified by Atlas: 384 tests, ruff clean, mypy 226 files, lint-imports 4 kept.
- D17 browser QA green: create 201 → edit etag CAS 201 (rev2, rev1 byte-identical) → stale etag 409 note_conflict → transform summarize 201 (real llama.cpp call, 2 provenance refs) → promote 201 (source version created+parsed, derived_from provenance edge in DB) → chat pin: pinned note answered 200, foreign-revision pin 422 grounding_invalid, unpinned answer does NOT mention note content (explicit-selection-only proven live).
- CRITICAL BUG PATTERN (fixed in 9cbc3a1): `@dataclass(frozen=True)` EXCEPTIONS raised inside `with engine.begin():` break — contextlib assigns `exc.__traceback__` on unwind, frozen __setattr__ turns it into TypeError → route except never matches → 500. Tests missed it because no test hit the conflict path. Rule: exceptions must be hand-written classes (T31 pattern), and every typed error path needs a live-propagation test.
- CSRF token survives API restarts AND browser crashes (it is a stable HMAC of the session token, config-keyed) — only client JS state is lost on reload.
- System-event recovery (17:01 crash killed PG+API+worker+web): PG restart = `/usr/lib/postgresql/17/bin/pg_ctl -D scratch/t13-smoke/pgdata -l scratch/t13-smoke/postgres.log -o "-p 29521 -k /home/srcds/dev/milpbookLM/scratch/t13-smoke" start`; then start-web/start-api/start-worker/start-embedding. Data intact.
- Visible-browser QA: killed playwright MCP (config had --headless in ~/.config/opencode/opencode.json; opencode does NOT hot-restart MCP servers) → launched headed Chrome manually with `--user-data-dir=<mcp profile> --remote-debugging-port=9222` (session cookie survives) → full playwright toolset via `skill_mcp(mcp_name="playwright", tool_name=..., arguments=..., cdp_url="http://localhost:9222")`. Tool names there have NO playwright_ prefix and args go in `arguments`.
- `notes` capability still published as `compiled_support_missing` (implemented=false) — inaccurate under-claim; flip to implemented=true/enabled=false (T31 artifact_lifecycle posture) with capabilities.yaml + capabilities.generated.json lockstep.

## 2026-09-23 onboarding and restored workspace state

- The reload-to-chat logout was a client CSRF restoration bug, not session loss: runtime logs showed `/auth/me` 200 followed by `POST /conversations` 403. Production sessions are PostgreSQL-backed, and CSRF is deterministically derived from the session token, so returning `csrf_token` from `/auth/me` restores unsafe-request authorization across reloads and API restarts without weakening middleware checks.
- A 403 is never proof that credentials are dead. CSRF/origin rejection and policy denial are actionable caller errors while the session remains valid; only a true 401 emits global session invalidation. Focused Vitest coverage proves restored `/auth/me` CSRF permits chat creation, 403 preserves auth, and 401 clears it.
- Registration-only onboarding belongs after `RegisterUser` succeeds at the HTTP/composition boundary. `SeedFeatureGuide` reuses `NotebookStore.create` and `CreateNote`, while the route catches and logs seed failures so account creation remains successful. It creates `Welcome to MilBook LM — Feature Guide` with seven editable revision-one notes and never runs on login or for existing users.
- Source reload persistence requires a real collection read path. `SourceCatalog.list` and `PgSourceCatalog.list` return the newest persisted version for each membership-visible source; the web merges those rows with optimistic session rows by `source_id:source_version_id`, preserving optimistic state while deduplicating exact versions.
- Registry edits need ID-specific patch context: an initial broad scalar replacement changed the adjacent `agentic_chat` entry. Diff review caught it before staging; the final lockstep change is only `notes` with `implementation_status=implemented`, `implemented=true`, and `enabled=false`.
- Final gates: 390 unit/meta/architecture tests passed; focused journey drivers passed 5 Python cases and all 23 web tests; Ruff clean; mypy clean across 227 files; import-linter kept 4 contracts; web production build passed; scoped Biome passed. The external basedpyright process still reports the known uv-workspace import-resolution errors documented in T22, while TypeScript LSP diagnostics are clean.
