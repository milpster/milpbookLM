## Capability + phase

<!-- Format: <capability> / phase <N> — e.g. "notebook_management / phase 1"
     (commit style: conventional type(scope): summary + requirement-ID refs) -->

- **Capability:**
- **Phase:**

## Requirement IDs

<!-- List the ARCH-CC-NNN / TECH-CC-NNN IDs this change implements or fixes.
     They must appear in milpbookml-implementation-guide/requirements.generated.json. -->

-

## Changes

<!-- What changed and why. One short paragraph per atomic concern. -->

## Tests & evidence

<!-- Positive + negative tests, verification IDs (VER-*) and evidence paths
     (artifacts/verification/VER-*.json) per 25-software-testing-release-engineering.md. -->

- [ ] Unit tests pass (`uv run pytest tests/unit`, `pnpm test`)
- [ ] Architecture checks pass (`uv run import-linter`, `pnpm exec tsc --build`)
- [ ] Evidence JSONs written and committed

## Deviations

<!-- Any deviation from the guide requires risk, compensation and a
     reconsideration point (00-status-decisions.md). Leave "None" if none. -->

None
