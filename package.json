{
  "name": "milpbooklm",
  "version": "0.1.0",
  "private": true,
  "description": "milpbookML monorepo (frontend pnpm workspace)",
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": "24.21.0"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "lint": "biome check .",
    "format": "biome format .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}
