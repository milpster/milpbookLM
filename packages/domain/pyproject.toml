[project]
name = "milpbooklm-domain"
version = "0.1.0"
description = "Pure entities, policies and state machines. No framework or adapter imports."
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/milpbooklm_domain"]
