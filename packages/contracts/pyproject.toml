[project]
name = "milpbooklm-contracts"
version = "0.1.0"
description = "OpenAPI, JSON Schema, event and provider contracts. Plain data, no frameworks."
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/milpbooklm_contracts"]
